public class BinarySearchPractice {
    public static void main(String[] args) {
        int[] sortedNumbers = {3, 6, 8, 12, 15, 18, 20, 25, 28}; // Sorted array of integers
        int target = 15; // Target number we are searching for in the array

        System.out.println("Target " + target + " found at index: " + binarySearch(sortedNumbers, target));
    }

    public static int binarySearch(int[] arr, int target) {
        // Implement the binary search logic here

    }
}